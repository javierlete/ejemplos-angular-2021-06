import { MayusculatorPipe } from './mayusculator.pipe';

describe('MayusculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
